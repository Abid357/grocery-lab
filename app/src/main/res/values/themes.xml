<resources>
    <!-- Base application theme. -->
    <style name="Theme.MyApplication" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/splashScreen</item>
        <item name="colorPrimaryVariant">@color/splashScreen</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/splashScreen</item>
        <item name="colorSecondaryVariant">@color/white</item>
        <item name="colorOnSecondary">@color/white</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
    </style>

    <style name="CustomSwitchTheme" parent="Theme.MaterialComponents">
        <item name="trackTint">@color/light_grey</item>
        <item name="thumbTint">@color/splashScreen</item>
        <item name="android:thumbTint">@color/splashScreen</item>
    </style>
</resources>